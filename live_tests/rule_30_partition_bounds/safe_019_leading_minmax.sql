CREATE TABLE parent (a integer, b integer) PARTITION BY RANGE (a, b);
CREATE TABLE child PARTITION OF parent FOR VALUES FROM (MINVALUE, MINVALUE) TO (10, 20);
ALTER TABLE parent DETACH PARTITION child CONCURRENTLY;